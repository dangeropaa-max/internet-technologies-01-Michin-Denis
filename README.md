
# Счётчик

# 1.
    Состояние хранится в одной переменной 'value'. Все изменения происходят только с ней.
# 2.   
    Интерфейс обновляет функция 'render()'. Она читает текущие значение 'value', записывает его в DOM и определяет текст сообщения.
# 3. 
    Да. В обработчиках;
        '+1': 'value += 1; render();'
        '-1': 'value -= 1; render();'
        'Сброс': 'value = 0 ; render();

# 4.            
    Внутреннее состояние программы изменится, а интерфейс останется прежним — они рассинхронизируются.

